# Session Progress Log

## Current State

**Last Updated:** 2026-08-10
**Session ID:** authkit-laravel-v1 execution (/goal-driven, direct build authorized by Nick)
**Active Feature:** Phase 11 (Pipes) committed; Phase 13 (Acceptance) is the only phase remaining

**Execution mode note:** The ideation plugin gates all execution skills (`execute-spec`, `autopilot`) behind human invocation (`disable-model-invocation`), so autopilot could not dispatch phases. Nick explicitly authorized direct spec implementation instead: sequential fresh-context subagents, one per phase in numeric order, each with an `ideation:scout` readiness gate, `composer test` green, an `ideation:reviewer` cycle (strict fail-closed, max 3), and a commit referencing the slug-qualified spec path.

## Status

### What's Done

- [x] feat-001 Project Setup: `./init.sh` green (2026-08-03)
- [x] Full ideation for AuthKit Laravel v1: interview to 5/5 gates, Mission Brief contract approved (Full tier, express finish), 4 plan critics run and folded, 13 implementation specs written + adversarially reviewed + fixed (all Strong at the feedback-quality gate)
- [x] Artifacts in `docs/ideation/authkit-laravel-v1/`: contract-data.json (Approved), contract.html, contract.md, spec-template-feature-area.md, spec-phase-1..13.md
- [x] Phase 1 (Foundation & Client Binding): commit `ba8ee8d`, composer test green (phpstan 0 errors, pint pass, 100% type coverage, pest 56/56, 123 assertions). Token-audit docs shipped with TBD findings pending a human run against a real WorkOS environment.
- [x] Phase 2 (Auth Core & Sealed Sessions): commit `b6f926e` (unsigned), 47 files/+3031, reviewer PASS after 3 cycles, composer test green (124 tests, 296 assertions, PHPStan level 7 clean, 100% type coverage)
- [x] Phase 3 (Organizations & Org Context): commit `18bb267` (unsigned), 37 files/+2013, reviewer PASS cycle 1 (1 medium + 2 low, mediums fixed in-cycle), composer test green (169 tests, 425 assertions, PHPStan level 7 clean, Pint clean, 100% type coverage, env-grep clean). Ran before Phase 5, so Component 7 step 0 "no" branch taken: `ResolvesOrganizationMembershipId` + `bindIf()` authored here; `WorkosGuard::accessTokenClaims()` thin accessor added per spec-phase-5 §4.1. Decision log: `docs/ideation/authkit-laravel-v1/implementation-notes-phase-3.html` (9 entries — includes two spec-snippet fatals fixed empirically: `static::observe()` in a boot hook, `$afterCommit` redeclaration vs Queueable). Baseline repair folded in: tests/TestCase.php pins app.key (skeleton .env purged by composer install made the 10 web-group tests fail on fresh checkout).
- [x] Phase 5 (Authorization RBAC + FGA): commit `6d919ae` (unsigned), 24 files/+1822, executed via /ideation:execute-spec --headless --strict, inline scout GO 5/5 + inline reviewer PASS cycle 1 (2 low, non-blocking), composer test green (215 tests, 544 assertions, PHPStan level 7 clean, Pint clean, 100% type coverage). ClaimsGateHook (Gate::before, never-false), HasAccessTokenClaims contract on WorkosGuard, RoleManager/PermissionManager/ResourceManager + Authkit::check() (FgaChecker, no cache), ResourceTarget DTO, HasWorkosResource trait + WorkosResource contract, WorkosResourcePolicy (__call), NullMembershipResolver + MembershipNotResolvedException. Both spec emulate Open Items empirically probed against @workos/emulate@0.6.0 → both sanctioned MockHandler fallbacks taken (emulate authorization surface drifted from SDK v9.1: check wants `permission` not `permission_slug`, assignRole wants `role_id`, role/permission/resource payloads unparseable by SDK fromArray); one emulate smoke test kept (empty-list round-trips). Phase 3's resolver default kept (seam already filled). Decision log: `docs/ideation/authkit-laravel-v1/implementation-notes-phase-5.html` (6 entries).

- [x] Phase 7 (Feature Flags, Pennant Driver): commit `be80a49` (unsigned), 6 files/+750, executed via /ideation:execute-spec --headless --strict, inline scout GO 5/5 + inline reviewer PASS cycle 1 (2 low, 1 fixed in-cycle, 1 spec-mandated observation), composer test green (230 tests, 593 assertions, PHPStan level 7 clean, Pint clean, 100% type coverage). laravel/pennant ^1.22 added to require (resolves v1.24.0); WorkosPennantDriver (read-only, claims-first with identity match, cache-fronted API fallback, 20x-TTL stale-serve, sha256 env-hash cache keys) + WorkosFeatureScope DTO; boot()-time dot-notation pennant.stores.workos injection (D-4 order race) + Feature::resolveScopeUsing pinned to the workos guard (D-5); guardClaims() consumes HasAccessTokenClaims per spec Open Item 2 against the landed guard. Suite MockHandler-only (15 cases) per the spec's emulate verb-mismatch designation. Decision log: `docs/ideation/authkit-laravel-v1/implementation-notes-phase-7.html` (5 entries).

- [x] Phase 9 (Vault): commit `b99b80d` (unsigned), 18 files/+1581, executed via /ideation:execute-spec --headless --strict, inline scout GO 5/5 + inline reviewer PASS cycle 1 (1 medium fixed in-cycle, 2 low non-blocking), composer test green (262 tests, 695 assertions, PHPStan level 7 clean, Pint clean, 100% type coverage; Vault suite 32 tests / 109 assertions, 100% MockHandler — emulate has zero Vault coverage). Vaulted cast + VaultCrypto + ResolvesVaultKeyContext seam (config-swappable, org duck-typing, fail-fast InvalidVaultKeyContextResolverException), VaultFilesystemAdapter decorator + VaultFileTooLargeException size guard + Storage::extend('vault') before the console early-return, VaultManager + Vault facade + composer alias. Spec Open Item 1 resolved: contract injection + bind() (not singleton) per Phase 1 harness. Codebase surprise: FilesystemManager::extend rebinds closures (RebindsCallbacksToSelf) — same trap as Auth::extend. Vault scope fully closed (no later phase revisits it). Decision log: `docs/ideation/authkit-laravel-v1/implementation-notes-phase-9.html` (9 entries). Known flake noted there: InstallIdempotentTest's --force config republish can race sibling parallel workers' app boot (pre-existing; fold into Phase 13 CI work).

- [x] Phase 4 (Events Pipeline & Webhooks): commit `806c5d6` (unsigned), 47 files/+2503, executed via /ideation:execute-spec --headless --strict, inline scout GO 5/5 + inline reviewer PASS cycle 1 (1 medium fixed in-cycle: update-side email collision could permanently poison the poller; 3 low non-blocking), composer test green (329 tests, 949 assertions, PHPStan level 7 clean, Pint clean, 100% type coverage, env-grep clean; phase suites: mapper 19, worker 13 incl. one emulate smoke on port 4195, resume 1, lock 2, webhooks 10, projections 17, generator 6). WorkosEventMapper (14 bounded types + GenericWorkosEvent fallback, shared by both transports), authkit:work poller (asc/after cursor walk, commit-after-dispatch at-least-once, rangeStart Y-m-d\TH:i:s.v\Z fallback, cache-lock singleton renewed via refresh()), Route::workosWebhooks macro + raw-body HMAC middleware (CSRF exclusion covers L12 ValidateCsrfToken AND L13 PreventRequestForgery via class_exists-filtered strings), 8 idempotent projection listeners (reconciled onto WorkosOrganizationDomain/WorkosMembership + singular authkit.organization.model + AuthKitController userModel chain), make:workos-listener generator, docs/quickstart.md created. Emulate findings: /events parses via EventSchema::fromArray but bogus `after` returns 200 (never 400/404) and range_start format unenforced → MockHandler-primary. Codebase surprises: Larastan boots workbench routes (bare macro call = silent phpstan exit-1; guarded with Route::hasMacro), trap() no-ops under runningUnitTests, DevCommands 13.24 deprioritizes (not refuses) vendor registrations. Decision log: `docs/ideation/authkit-laravel-v1/implementation-notes-phase-4.html` (11 entries).

- [x] Phase 8 (API Keys Guard): commit `f5760da` (unsigned; 1Password signer failed with "failed to fill whole buffer" — same unsigned precedent as Phases 2-9), 20 files/+1443, executed via /ideation:execute-spec --headless --strict, inline scout GO 5/5 + inline reviewer PASS cycle 1 (1 medium + 3 low, all non-blocking/logged), composer test green (362 tests, 1056 assertions, PHPStan level 7 clean, Pint clean, 100% type coverage, workbench SDK-free grep clean; phase suites: emulate journey 2 on port 4193, MockHandler 20, actor/hook unit 11). authkit-key guard (ApiKeyAuthenticator: fresh createValidation per request, fail-closed on outage, bearer or literal-header extraction, data-shadow warnings, MissingModelConfigurationException naming real config keys) + ApiKeyRequestGuard (RequestGuard subclass — stock setRequest keeps the memoized user, leaking request #1's principal to request #2; WorkosGuard::setRequest precedent) + runtime auth.guards.authkit-key default (consumer wins); ApiKeyGateHook mirrors ClaimsGateHook (true/null, never false, mutually exclusive permission sources vs Phase 5); HasApiKeys (User) + HasOrganizationApiKeys (org) sharing revokeApiKey via InteractsWithWorkosApiKeys; ApiKeyCreated/ApiKeySummary DTOs make raw-value-once structural. **expireApiKey CUT** per spec §1/§11 scope-expansion directive: contract-data.json's only "expire" mentions are JWT expiry, no transcript confirms the phase-direction claim. Emulate 0.6.0 empirical findings: NO org-key create endpoint (create → MockHandler wire tests); array-form apiKeys seed REPLACES the default allow-list → dedicated seed fixture (workos-emulate-api-keys.config.yaml) re-seeds sk_test_default; pinned org ids let local projections match owners. Decision log: `docs/ideation/authkit-laravel-v1/implementation-notes-phase-8.html` (5 entries). Reviewer lows carried: listApiKeys maps first page only (spec-conformant; autoPagingIterator candidate for a later phase).

- [x] Phase 10 (Connect & MCP Auth): commit `8ba2f62` (unsigned; 1Password signer "failed to fill whole buffer" — same precedent as Phases 2-9), 26 files/+1826, executed via /ideation:execute-spec --headless --strict, inline scout GO 5/5 + inline reviewer PASS cycle 1 (1 medium + 3 low, all non-blocking/logged), composer test green (402 tests, 1173 assertions, PHPStan level 7 clean, Pint clean, 100% type coverage; connect-mcp group 40 tests/117 assertions). Authkit::connect() ConnectManager (scalar-only boundaries both directions, blank-org fail-fast, create-then-delete rotation, Idempotency-Key passthrough) + 3 DTOs + ConnectException; authkit.mcp middleware (RFC 6750 challenge split, iss/aud policy, 503-not-401 on cold-cache JWKS outage, opt-in resolve_user by sub); RFC 9728 /.well-known/oauth-protected-resource (soft-404 unconfigured, above the routes.enabled gate); laravel/mcp ^0.9.1 (resolves 0.9.2) workbench recipe (Mcp::web + authkit.mcp, loadRoutesFrom(workbench_path)). KEY RECONCILIATION: spec §4.1's assumed Phase 2 JwksVerifier did not exist (Phase 2 delegates to SDK SessionManager; JwksGraceCache is /sso/jwks/-scoped transport grace) — authored Support/Jwt/JwksVerifier here to the exact §4.1 contract (RS256 allow-list pre-fetch, debounced kid-miss refresh, cache-TTL fetch) + JwtVerificationException/JwksUnavailableException split. Reused Phase 2's JwtFixture (additive $headerOverrides) instead of SelfSignedJwksFixture per spec Open Item 2. Env rename: AUTHKIT_MCP_RESOURCE_INDICATOR → WORKOS_MCP_RESOURCE_INDICATOR (Phase 1 placeholder, zero consumers). Upstream finding: laravel/mcp AddWwwAuthenticateHeader rewrites 401 challenges on Mcp::web routes (drops resource_metadata pointer) — Phase 13 README callout. Decision log: `docs/ideation/authkit-laravel-v1/implementation-notes-phase-10.html` (8 entries).

- [x] Phase 6 (Audit Logs & Admin Portal): commit `f99c933` (unsigned — same 1Password precedent as Phases 2-10), 32 files/+2021, executed via /ideation:execute-spec --headless --strict, inline scout GO 5/5 + inline reviewer PASS cycle 1 (1 medium + 2 low, all documented deviations, non-blocking), composer test green (449 tests, 1357 assertions, PHPStan level 7 clean, Pint clean, 100% type coverage; all 7 spec §10 filters green — HasAuditLogsTrait 6, AuditLogContext 5, AuditLogFacade 4, AuditLogSchema 5, AuditLogExport 7, AdminPortal 5, OrganizationDomainVerification 5). HasAuditLogs trait (create/update/archive-on-soft-delete/delete-on-force-delete/restore; property > #[AuditActions] attribute > slug default, partial merges; restored via registerModelEvent so non-SoftDeletes models boot) + AuditLog facade (eager actor/org resolution from workos-guard claims, queued CreateAuditLogEventJob with per-call Idempotency-Key UUID, MetadataSanitizer 50-key/500-char caps with warning, AuditLogSchemaMismatchException on 4xx) + schema/export/retention exact passthroughs (createExport auto-dispatches bounded PollAuditLogExportJob → AuditLogExportReady/AuditLogExportFailed; setRetention validates 30|365 locally) + Authkit::portalLink() over the 7-case PortalIntent enum (string org id or workos_id model, unsynced-model fail-fast). KEY RECONCILIATIONS: Open Item 1 — Authkit::currentOrganizationId() never landed, resolver reads org_id claim via HasAccessTokenClaims; §4.8 — verification-pair ownership transferred from Phase 4's UpsertOrganizationDomainProjection (whose landed tests contradicted §4.8: token kept, no failed state, rows created) to the new UpdateOrganizationDomainVerificationState (state stamping, token clearing, warn+no-op unknown rows); AuditLogManager bind() not singleton (MockHandler swap, Phase 9 precedent). Emulate 0.6.0 empirical: createEvent body shape drift (SDK nests under `event`, emulate reads top-level `action`), action/schema responses unparseable by SDK fromArray (no `schema` key), listActionSchemas + PUT retention routes absent → MockHandler-primary with one emulate wire smoke (port 4191); AdminPortal fully emulate-backed (port 4192). Decision log: `docs/ideation/authkit-laravel-v1/implementation-notes-phase-6.html` (5 entries). Known pre-existing flake (unchanged by this phase): rare parallel-worker `mergeConfigFrom array_merge(): Argument #2 must be of type array, int given` (hit EventsWorkerTest at baseline, FeatureFlagsTest once mid-phase; both re-runs green — Phase 13 CI candidate alongside the InstallIdempotentTest race).

- [x] Phase 12 (Depth Extensions, Full Tier): commit `746a3de` (unsigned — same 1Password precedent as Phases 2-10), 23 files/+2043, executed via /ideation:execute-spec --headless --strict, inline scout GO 5/5 + inline reviewer PASS cycle 2 of 3 (cycle 1: 1 high — cache-store resolution sat outside FgaChecker's fail-open guard, so a misconfigured `authkit.fga.cache.store` would break checks instead of degrading to a live call [spec Failure Mode 3]; fixed + regression test. 2 low carried, non-blocking: LRU eviction of the generation counter / no TTL on it — both inside the documented TTL staleness bound), composer test green (499 tests, 1546 assertions, PHPStan level 7 clean, Pint clean, 100% type coverage; all six spec filters green — Invitations 3 [emulate port 4194], JwtTemplate 3 [emulate port 4190], CorsOrigins 3, Groups 14, FgaResourceGraph 9, FgaCache 18; `--group=depth-extensions` runs them all). Authkit::invitations()/jwtTemplate()/corsOrigins()/groups()/fga() accessors (app()-resolution pattern; managers constructor-inject the WorkosClientManager CONTRACT, auto-wired non-singleton). Opt-in FGA check cache under `authkit.fga.cache.{enabled,ttl,store}` (disabled by default): FgaChecker::check() signature unchanged, SDK call renamed rawCheck(), generation-versioned keys (`authkit:fga:check:g{n}:{om}:{perm}:{fragment}`, O(1) invalidation via counter increment, tag-free = driver-portable), every cache fault fails open + logs; InvalidateFgaCache listens on typed membership events AND GenericWorkosEvent filtered to role./organization_role./permission./group. prefixes (catalog has no role_assignment/authorization_resource events — TTL-only bound documented). KEY DEVIATIONS (all logged): listeners registered UNCONDITIONALLY (forgetCache() is the config-guarded no-op; boot-time gating would pin the flag and detach invalidation on runtime config flips); JwtTemplateManager::update() catches NotFoundException on the before-read (real WorkOS 404s until a template ever exists — spec-literal would break the first write; previousContent ''); HasWorkosResource parent guard is `instanceof WorkosResource` + returns package ResourceTarget (spec snippet's `class_uses_recursive(self::class)` is broken inside a trait; SDK types stay behind the boundary via new toParentTarget()); GroupManager::roleAssignment() getter added (spec's wrapped-methods table lists getGroupRoleAssignment; snippet omitted it); workbench demo routes read organization_id from the query (workbench.demo_organization_id never landed — spec Open Item 5). Emulate 0.6.0 empirical: invitations SOLID (all 7 routes SDK-compatible; drift: resend skips the pending check → error case uses revoke-on-revoked → 400 BadRequestException); jwt_template GET 404s until seeded (dedicated seed workos-emulate-depth.config.yaml pins org_01DEPTH + jwtTemplate so the shared fixture never grows token claims); CORS has create but NO list route, groups have no routes → MockHandler with exact wire assertions (spec Open Item 2 resolved). Decision log: `docs/ideation/authkit-laravel-v1/implementation-notes-phase-12.html` (8 entries). Residual known gap (spec Failure Mode 2 / Open Item 3, out of scope by design): Phase 5's RoleManager RBAC assignment methods don't proactively bust the cache — natural Phase 13-or-later patch mirroring GroupManager's forgetCache() calls.

- [x] Phase 11 (Pipes): commit `c9fd893` (unsigned — same 1Password precedent as Phases 2-12), 14 files/+1025, executed via /ideation:execute-spec --headless --strict, inline scout GO 5/5 + inline reviewer PASS cycle 1 (1 medium documented deviation + 2 low, all non-blocking), composer test green (514 tests, 1625 assertions, PHPStan level 7 clean, Pint clean, 100% type coverage; Pipes suite 15 tests/79 assertions via `--filter=Pipes`). Authkit::pipes() PipesManager (connectedAccounts live read-through mapping listUserDataProviders with never-connected filtering, accessToken with WorkOS-managed refresh, providerConfig/configureProvider PipesProvider passthrough) + sealing enums ConnectedAccountState/AuthMethod + 3 DTOs (ConnectedAccountData/PipeAccessTokenData/ProviderConfigurationData, clientSecretLastFour redacted-only) + 2 named exceptions (PipesAccountNotConnectedException for not_installed — never conflated with outage passthrough; PipesReauthorizationRequiredException unifying hard error=needs_reauthorization and soft missing_scopes, carrying the authorize URL fetched eagerly on the exceptional branch only). **SCOPE RULING (spec §2 traceability flag): connect()/disconnect()/import() + PipesInvalidImportException TRIMMED** per the spec's own no-ratification directive — contract names exactly three capabilities, no ratifying artifact exists, Phase 8 expireApiKey CUT precedent; deferred to a future contract amendment. HasWorkosUser gains connectedAccounts()/pipe() pure delegations behind a getAttribute('workos_id') guard (fails loudly pre-HTTP for never-linked users). No provider binding — auto-wired non-singleton (Phase 12 precedent; spec §6 singleton would pin the pre-fake client manager). Workbench gains its first controller (PipesController: trait list, reauth-redirect token flow with raw token never echoed, org passthrough; zero SDK references) + 4 routes. Suite MockHandler-only per Decision #2 (emulate has zero Pipes coverage). Spec Open Items resolved: 1 → client() contract accessor as assumed; 2 → UsesWorkosMockHandler::fakeWorkosResponses (no fake() method exists); 3 → moot after trim; 4 → trait path as assumed. Decision log: `docs/ideation/authkit-laravel-v1/implementation-notes-phase-11.html` (5 entries).

- [x] Phase 13 (Integration, Quickstart & Release Readiness): commit `5521e4c` (unsigned — same 1Password precedent as Phases 2-12), 27 files/+1407, executed via /ideation:execute-spec --headless --strict, inline scout GO 5/5 + independent reviewer (claude -p, read-only tools, plugin reviewer definition) PASS cycle 1 AND cycle 2 of 3, both zero findings (cycle 2 re-reviewed the guard-entry delta), composer test green (530 tests, 1693 assertions, PHPStan level 7 clean, Pint clean, 100% type coverage). Workbench build-out closes every scope-table gap (Dashboard hub + AuditLog/AdminPortal/RBAC+FGA/FeatureFlags/ApiKeys/Vault/ConnectMcp demo routes, demo:feature-flags + demo:trigger-event commands, LogWorkosEvents two-branch listener recipe) — all package-API-only, enforced by the new WorkbenchZeroSdkReferenceTest (grep wire-identical to the contract check, negative case proven). AcceptanceTest runs the full contract journey against live emulate on port 4189 with a dedicated seed (login → workos_id link → external_id set in WorkOS → org+membership projection → can()-from-claims true/false via real HTTP, cold AND warm logins); ProjectionBoundaryTest whitelists the 5 declared projections bidirectionally (negative proven); IdiomCoverageTest resolves all promised mechanisms through Laravel's own managers, closing both spec Open-Item gaps honestly (workosWebhooks macro asserted; no Blade directive exists in any landed phase — Gate::before hooks probed behaviorally instead of faking a directive name). TWO FOUND BUGS fixed under the spec's pre-authorized clause: AuthkitConfig::baseUrl() ignored the emulate override (guard JWKS verification + logout URLs hit production WorkOS with emulate on — first-ever emulate-authenticated guard test surfaced it) and the missing default auth.guards.workos entry (the Known Gap below, authkit-key merge pattern, consumer wins, both branches tested). docs/quickstart.md rewritten: 5 numbered steps (≤5 ceiling; spec's 4-step draft assumed the installer wires auth — it doesn't, so an honest wiring step was added), Phase 4 content retained unnumbered. docs/release-checklist.md carries the human-trial log (left blank on purpose — never fabricate). README + Boost skill regenerated from the real implementation. feature_list.json now the real 13-phase roadmap, all statuses evidence-backed. CI: ubuntu-only emulate priming step (npx warm + --validate-config over all 4 seeds) replaces the spec's external-boot design (per-suite EmulateServer is the landed architecture; spec Open Item 6's simplify branch). Decision log: `docs/ideation/authkit-laravel-v1/implementation-notes-phase-13.html` (10 entries).

### What's In Progress

- (none — all 13 phases committed; project is release-pending on the human gates below)

### Known Gaps To Fold In Later

- [x] ~~authkit:install does not write config/auth.php guard/provider entries~~ RESOLVED in Phase 13 (`5521e4c`): AuthkitServiceProvider::register() seeds a default `auth.guards.workos` entry (users provider, consumer entry wins) mirroring the authkit-key pattern; IdiomCoverage tests cover both branches
- [ ] jwt.issuer ships with null default; iss enforcement turns on via WORKOS_JWT_ISSUER once the human token audit confirms the canonical value (spec-phase-2 Open Item 1)

### What's Next (release gates — human, not agent, work)

1. ~~Re-sign the unsigned chain~~ DONE 2026-08-10 — full Birdcar chain (31 commits, 4b1a8db→HEAD) signed via `git rebase --exec 'git commit --amend -S --no-edit' 0bdfa7e`; verified zero unsigned; recorded hashes updated in b18da9f
2. ~~Push and watch the CI matrix~~ DONE 2026-08-10 — tests.yml run 31396574476 on commit 2850b41: all 24 matrix jobs green (PHP 8.3–8.5 × Laravel 12/13 × prefer-lowest/prefer-stable × ubuntu/windows). Five environment-specific fixes were needed to get there, each with the mechanism in its commit message: ad2d044 (Laravel 12 floor: 13.x-only Eloquent attributes in workbench models, Lock::refresh() runtime fatal in authkit:work, larastan dev floor → ^3.10), 278c004 (PHP 8.3/8.4: Connect registration_types enums exploded by http_build_query pre-8.5), 1a29b9d (Windows: PHPStan parallel workers race the shared Testbench skeleton's provider manifest — single-process via phpstan-windows.neon), 2be03f7 (Windows: read-only env fixture cannot be unlinked in teardown), 2850b41 (CI: pest-plugin-type-coverage Pokio forks corrupt their shared .temp cache — FORK_IO_FACTOR=-1 serializes them on runners)
3. ~~Run the human quickstart trial~~ WAIVED 2026-08-13 by Nick for v2.0.0 ("I'm building a starter kit with it and that will be the post publish trial with patches if needed") — the checklist table stays blank (never fabricate); the waiver is disclosed in the v2.0.0 release notes. The starter kit build is the live trial; log findings as issues/patches.
4. Run the Phase 1 token audit against a real WorkOS environment (docs/token-audit.md findings still TBD; WORKOS_JWT_ISSUER enforcement stays opt-in until then)
5. Draft CHANGELOG/release notes, then walk docs/release-checklist.md top to bottom before tagging — PARTIAL 2026-08-13: release decided as **v2.0.0** (v1.x line on Packagist is the old codebase; a confused agent tagged v1.0.1 from the pre-rebuild history as a workos-php v5 hotfix — its `release/v1.0.1` branch and the stale `release-please--branches--main` branch were deleted from origin; the v1.0.1 tag stays as the v1-line capstone). CHANGELOG.md skeleton fixed (real dates, v1.0.1 hotfix entry, Unreleased → v1.0.1...main). v2.0.0 release notes drafted (session scratchpad; body to be passed to `gh release create` — update-changelog.yml copies it into CHANGELOG.md on publish). Mechanical checklist walked, all green: composer test 12.8s cold (530 tests/1693 assertions, PHPStan clean, 100% type coverage), Acceptance/ProjectionBoundary/IdiomCoverage/WorkbenchZeroSdkReference filters pass, quickstart 5 steps, 51 feature test files, no env() in src, no SDK refs in workbench. Local-only fix: orphaned nunomaduro/phpinsights chain pruned from composer.lock (gitignored) — it caused the "dealerdirect/phpcodesniffer-composer-installer plugin was not loaded" warning on every composer run. DONE 2026-08-13: gate 3 waived by Nick (see above), publish explicitly approved — signed tag v2.0.0 pushed, GitHub release live (https://github.com/birdcar/authkit-laravel/releases/tag/v2.0.0), update-changelog.yml green (release body copied into CHANGELOG.md).

## Blockers / Risks

- [x] ~~**Commits from Phase 2 onward are unsigned**~~ RESOLVED 2026-08-10: full Birdcar chain re-signed from 0bdfa7e with 1Password unlocked (31 commits, all verified signed); upstream skeleton commits untouched
- [x] ~~**origin/main was force-pushed and shares NO common ancestor with local main**~~ RESOLVED 2026-08-10: the unrelated remote history was the old repo, and Nick deliberately force-pushed this local history over it ("I intended this one to replace that one"); origin/main now tracks this chain and plain pushes work.

- [ ] Phase 1 must complete the empirical AuthKit token audit (canonical iss/aud values + default claim presence) before Phase 2 starts — encoded in the specs
- [ ] Execution runs directly on `main` by stakeholder decision; recovery anchor for a bad run is the pre-execution tip (the handoff commit — last commit before Phase 1), which preserves the committed plan

## Decisions Made

- Full decision log (19 entries) lives in `docs/ideation/authkit-laravel-v1/contract-data.json` → decisions; highlights: sealed-session guard (WorkOS canonical), RBAC via JWT claims zero-HTTP + FGA Check API, emulate-in-CI truth bar with MockHandler fallback, breadth-complete v1 at Full tier, Pennant driver for flags, no isolation branch (main, repo exists on GitHub with local main 3 ahead — premise corrected and re-confirmed)

## Files Modified This Session

- `docs/ideation/authkit-laravel-v1/*` — all new (contract + 13 specs + shared template)
- No src/config/test changes; package code untouched this session

## Evidence of Completion

- [x] Contract gates 5/5 with evidence; 15 success criteria (14 mechanical)
- [x] Spec quality gate: 13/13 specs Strong after adversarial review + fixes (2 workflow runs, 57 agents; phase-2's final finding hand-fixed and vendor-verified)

## Notes for Next Session

If you are the /goal execution session: run `./init.sh` first, then `/ideation:autopilot docs/ideation/authkit-laravel-v1/contract.md`. Specs are standalone; the feature-area template (`spec-template-feature-area.md`) is required reading for phases 5–12. The ideation artifacts are already committed, so spec paths referenced in phase commit messages exist in history.
