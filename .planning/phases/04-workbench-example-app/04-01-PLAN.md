---
phase: 04-workbench-example-app
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - workbench/composer.json
autonomous: true
requirements:
  - WORK-06
  - WORK-07
must_haves:
  truths:
    - "workbench/auth.json is ignored by git at root level"
    - "workbench/composer.json requires PHP ^8.3"
  artifacts:
    - path: ".gitignore"
      provides: "Root-level auth.json exclusion"
      contains: "workbench/auth.json"
    - path: "workbench/composer.json"
      provides: "PHP version constraint"
      contains: "\"php\": \"^8.3\""
  key_links: []
---

<objective>
Fix two mechanical compliance gaps: add workbench/auth.json to root .gitignore (WORK-06) and bump PHP constraint from ^8.2 to ^8.3 in workbench/composer.json (WORK-07).

Purpose: Satisfy credential protection and PHP version alignment requirements before any functional changes.
Output: Updated .gitignore and workbench/composer.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.gitignore
@workbench/composer.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add auth.json to root .gitignore and fix PHP constraint</name>
  <files>.gitignore, workbench/composer.json</files>
  <read_first>.gitignore, workbench/composer.json</read_first>
  <action>
1. In `.gitignore`, add `workbench/auth.json` entry at the end of the file (after the `docs/ideation/**` line). This is a belt-and-suspenders measure — `workbench/.gitignore` already has `/auth.json` but the root gitignore provides defense-in-depth per WORK-06.

2. In `workbench/composer.json`, change the PHP constraint from `"php": "^8.2"` to `"php": "^8.3"` in the `require` section. This aligns workbench with the package's own `^8.3` constraint per WORK-07.

Do NOT run `composer update` inside workbench — the lockfile is gitignored and not committed.
  </action>
  <verify>
    <automated>grep -q 'workbench/auth.json' .gitignore && grep -q '"php": "\^8.3"' workbench/composer.json && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- `grep 'workbench/auth.json' .gitignore` returns a match
- `grep '"php": "\^8.3"' workbench/composer.json` returns a match
- `git check-ignore workbench/auth.json` exits 0
  </acceptance_criteria>
  <done>Root .gitignore contains workbench/auth.json entry. workbench/composer.json requires PHP ^8.3.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Git tracking | Credential files must not be committed to version control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | workbench/auth.json | mitigate | Add to root .gitignore so Flux Pro Composer credentials are never committed even if placed at wrong path |
</threat_model>

<verification>
- `grep 'workbench/auth.json' .gitignore` matches
- `grep '"php": "\^8.3"' workbench/composer.json` matches
</verification>

<success_criteria>
WORK-06 satisfied: auth.json excluded from git at root level.
WORK-07 satisfied: PHP constraint is ^8.3.
</success_criteria>

<output>
After completion, create `.planning/phases/04-workbench-example-app/04-01-SUMMARY.md`
</output>
