---
phase: 01-inertia-middleware
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Http/Middleware/ShareWorkOSData.php
  - src/WorkOSServiceProvider.php
  - tests/Feature/InertiaMiddlewareTest.php
autonomous: true
requirements:
  - INRT-01
  - INRT-02

must_haves:
  truths:
    - "An Inertia app can read user, org, roles, permissions, and impersonation state from shared auth props"
    - "Installing the package in a non-Inertia app does not trigger any Inertia-related errors or warnings"
    - "The workos.inertia middleware alias resolves to a working ShareWorkOSData class"
    - "Sensitive fields (accessToken, refreshToken, expiresAt, sessionId) are not present in shared props"
  artifacts:
    - path: "src/Http/Middleware/ShareWorkOSData.php"
      provides: "Middleware implementation with class_exists guard and lazy Inertia::share()"
      contains: "class_exists(Inertia::class)"
    - path: "src/WorkOSServiceProvider.php"
      provides: "workos.inertia alias registration"
      contains: "workos.inertia"
    - path: "tests/Feature/InertiaMiddlewareTest.php"
      provides: "7 tests covering auth/unauth/impersonation/alias states"
      min_lines: 200
  key_links:
    - from: "src/WorkOSServiceProvider.php"
      to: "src/Http/Middleware/ShareWorkOSData.php"
      via: "aliasMiddleware('workos.inertia', ShareWorkOSData::class)"
      pattern: "workos\\.inertia.*ShareWorkOSData"
    - from: "src/Http/Middleware/ShareWorkOSData.php"
      to: "Inertia::share()"
      via: "closure-based lazy evaluation"
      pattern: "fn \\(\\) => \\$this->getAuthData"
---

<objective>
Verify that Phase 1 implementation is complete and all quality gates pass.

Purpose: The ShareWorkOSData middleware and workos.inertia alias are already implemented. This plan audits the implementation against INRT-01 and INRT-02 requirements, confirms the full quality gate suite passes, and marks the phase complete.
Output: Confirmation that INRT-01 and INRT-02 are satisfied by the existing implementation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-inertia-middleware/01-CONTEXT.md
@.planning/phases/01-inertia-middleware/01-RESEARCH.md

<interfaces>
<!-- Key implementation details verified by the researcher. No codebase exploration needed. -->

From src/Http/Middleware/ShareWorkOSData.php (72 lines, fully implemented):
```php
class ShareWorkOSData
{
    public function __construct(private readonly SessionManager $session) {}

    public function handle(Request $request, Closure $next): Response
    {
        if (! class_exists(Inertia::class)) {   // INRT-02: soft dependency guard
            return $next($request);
        }
        Inertia::share(['auth' => fn () => $this->getAuthData($request)]);  // INRT-01: lazy share
        return $next($request);
    }
}
```

Authenticated prop shape (per D-04, D-05):
```php
[
    'check'        => true,
    'user'         => ['id', 'workos_id', 'name', 'email'],  // no tokens (D-05)
    'roles'        => [...],
    'permissions'  => [...],
    'organization' => $session?->organizationId,
    'impersonating'=> $session?->impersonator !== null,
    'impersonator' => $session?->impersonator,
]
```

From src/WorkOSServiceProvider.php:192:
```php
$router->aliasMiddleware('workos.inertia', ShareWorkOSData::class);  // D-02
```

Quality gates (all verified by researcher):
- composer test      → 295 passed, 0 failed (7 in InertiaMiddlewareTest)
- composer analyse   → PHPStan level 8, 0 errors
- composer format:test → Pint, pass
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit implementation against INRT-01 and INRT-02</name>
  <files>src/Http/Middleware/ShareWorkOSData.php, src/WorkOSServiceProvider.php</files>
  <read_first>
    - src/Http/Middleware/ShareWorkOSData.php — confirm class_exists guard (line ~21), closure-based Inertia::share() (line ~25), full prop shape including impersonator key (lines ~57-70), absence of accessToken/refreshToken/expiresAt/sessionId
    - src/WorkOSServiceProvider.php — confirm aliasMiddleware('workos.inertia', ShareWorkOSData::class) exists in configureMiddleware()
  </read_first>
  <action>
    Read both files. Verify the following conditions are met — no code changes are expected:

    INRT-01 check: `getAuthData()` returns an array containing all required keys: `check`, `user` (with `id`, `workos_id`, `name`, `email`), `roles`, `permissions`, `organization`, `impersonating`, `impersonator`. Confirm the closure `fn () => $this->getAuthData($request)` is passed to `Inertia::share()` (lazy evaluation per D-06).

    INRT-02 check: `class_exists(Inertia::class)` guard is at the top of `handle()` before any Inertia call. Confirm `inertiajs/inertia-laravel` does NOT appear in composer.json `require` or `require-dev` sections.

    D-05 check: Confirm `accessToken`, `refreshToken`, `expiresAt`, `sessionId` are NOT keys in the returned array from `getAuthData()`.

    D-02 check: Confirm `$router->aliasMiddleware('workos.inertia', ShareWorkOSData::class)` exists in WorkOSServiceProvider.

    If any condition is NOT met, note the specific gap — but do NOT modify the files. Report the gap in the task output for human review before any fix is attempted.
  </action>
  <verify>
    <automated>grep -n "class_exists" /Users/birdcar/Code/birdcar/authkit-laravel/src/Http/Middleware/ShareWorkOSData.php && grep -n "workos.inertia" /Users/birdcar/Code/birdcar/authkit-laravel/src/WorkOSServiceProvider.php && grep -n "accessToken\|refreshToken\|expiresAt\|sessionId" /Users/birdcar/Code/birdcar/authkit-laravel/src/Http/Middleware/ShareWorkOSData.php | grep -v "use\|import\|//"</automated>
  </verify>
  <acceptance_criteria>
    - `class_exists(Inertia::class)` present in ShareWorkOSData::handle()
    - `workos.inertia` alias mapped to ShareWorkOSData::class in WorkOSServiceProvider
    - `accessToken`, `refreshToken`, `expiresAt`, `sessionId` do NOT appear as array keys in getAuthData() return values
    - All 7 required prop keys present: check, user, roles, permissions, organization, impersonating, impersonator
  </acceptance_criteria>
  <done>Both files read, all INRT-01/INRT-02/D-02/D-05/D-06 conditions confirmed present in source. No modifications made.</done>
</task>

<task type="auto">
  <name>Task 2: Run full quality gate suite</name>
  <files>tests/Feature/InertiaMiddlewareTest.php</files>
  <read_first>
    - tests/Feature/InertiaMiddlewareTest.php — review all 7 tests to understand what behaviors are covered before running
  </read_first>
  <action>
    Run the three quality gate commands in sequence. All must pass with zero failures or errors.

    Step 1 — Run the InertiaMiddleware test suite in isolation to confirm all 7 tests pass:
    ```
    composer test -- --filter=InertiaMiddleware
    ```
    Expected: 7 tests, 0 failures. Tests cover: passthrough when Inertia absent, unauthenticated shape, authenticated shape, impersonation data, user without getWorkOSId, null session, alias registration.

    Step 2 — Run the full test suite to confirm no regressions:
    ```
    composer test
    ```
    Expected: 295 tests, 0 failures.

    Step 3 — Run PHPStan static analysis:
    ```
    composer analyse
    ```
    Expected: 0 errors at level 8.

    Step 4 — Run Pint code style check:
    ```
    composer format:test
    ```
    Expected: pass (no style violations).

    If any step fails, do NOT attempt fixes. Report the exact failure output for human review.
  </action>
  <verify>
    <automated>cd /Users/birdcar/Code/birdcar/authkit-laravel && composer test -- --filter=InertiaMiddleware 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `composer test -- --filter=InertiaMiddleware` exits 0 with exactly 7 tests passed
    - `composer test` exits 0 with 295 tests passed, 0 failed
    - `composer analyse` exits 0 with "No errors" output
    - `composer format:test` exits 0
  </acceptance_criteria>
  <done>All four quality gate commands return exit code 0 with zero failures/errors. Phase 1 implementation confirmed complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| middleware → Inertia props | Auth state crosses from server session into frontend-readable shared props |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | ShareWorkOSData::getAuthData() | mitigate | Sensitive fields (accessToken, refreshToken, expiresAt, sessionId) explicitly excluded from shared props per D-05 — verified by Task 1 audit |
| T-01-02 | Information Disclosure | Inertia::share() | accept | Shared props are only evaluated when Inertia renders a response — closure-based lazy evaluation means JSON/API responses never trigger prop exposure |
| T-01-03 | Tampering | workos.inertia alias | accept | Alias is registered server-side in service provider; no client-controlled path to override it |
</threat_model>

<verification>
Phase 1 is complete when:
1. Task 1 confirms all source-level conditions (INRT-01, INRT-02, D-02, D-05, D-06) are met by reading the files
2. Task 2 confirms all four quality gates pass (7 InertiaMiddleware tests, 295 full suite, PHPStan level 8, Pint)
3. No code modifications were required
</verification>

<success_criteria>
- INRT-01: ShareWorkOSData shares check/user/roles/permissions/organization/impersonating/impersonator via Inertia::share() closure
- INRT-02: class_exists(Inertia::class) guard in handle(); no hard Inertia dependency in composer.json
- All 7 InertiaMiddlewareTest tests pass
- Full suite: 295 tests, 0 failures
- PHPStan level 8: 0 errors
- Pint: pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-inertia-middleware/01-01-SUMMARY.md` using the summary template.
</output>
